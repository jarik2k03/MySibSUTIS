#include <stdio.h>
#include <stdlib.h>
#include "graph.h"

int main()
{
    // int e, n;
    // printf("Random graph generation: ");
    // n = 7 + rand()%6;
    // printf("\nThe graph has %d vertices", n);  
    // e = rand()%((n*(n-1))/2);
    // printf("\nand has %d edges.", e);
    // GenRandomGraphs(e, n);
    work();
}
